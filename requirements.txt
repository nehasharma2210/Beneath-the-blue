# Core Requirements
Django==4.2.7
django-crispy-forms==2.1
Pillow==10.4.0  # For image processing
python-dotenv==1.0.0  # For environment variables

# Database
psycopg2-binary==2.9.7  # PostgreSQL adapter (remove if using SQLite)
dj-database-url==2.1.0  # For parsing DATABASE_URL environment variable

# Deployment (Essential only)
gunicorn==21.2.0
whitenoise==6.5.0  # Static files serving
